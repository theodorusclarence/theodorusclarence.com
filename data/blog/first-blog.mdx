---
title: 'This is my first mdx blog'
publishedAt: '2018-04-04'
description: 'This really works!'
---

```jsx
const name = 'hellow';
const lorem30 = 'lorem imsdifouhwe fiausdhf wesksis adfis adifuhekjf piauhsdfkje fsiuhekrjh ssfkjh'
<div class='ha'>waw</div>;
```

```bash
yarn add nodemon
```

-   makan
-   minum
-   kenyang

*   satu
*   dua
*   tiga

1. satu
2. dua
3. tiga

## Hello

[x] makan
[ ] minum

<Image src='/images/foto.jpg' width={300} height={400} />

## heading

<a href='/'>makan</a>

```jsx
export function getStaticProps() {
    const posts = postFilePaths
        .filter((filePath) => {
            const slugPath = filePath.replace(/\.mdx?$/, '');
            return featured.find((feature) => feature === slugPath);
        })
        .map((filePath) => {
            const source = fs.readFileSync(path.join(BLOGS_PATH, filePath));
            const { content, data } = matter(source);
            const slug = filePath.replace(/\.mdx?$/, '');

            return {
                content,
                data,
                filePath,
                slug,
            };
        });

    return { props: { posts } };
}
```
